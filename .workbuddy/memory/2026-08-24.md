# 2026-08-24

## 工程目录重组后修复路径（zhibodou AI 直播管控系统）
- 真实工程位置：`E:\zhibodou-ai\zhibodou`（注意：WorkBuddy 默认 workspace 是 C:\...\zhibodou-ai，但实际代码在 E 盘）。
- 目录结构：`src/`(主程序+启动bat+admin_pwd.dat) / `scrcpy/`(scrcpy.exe, adb.exe 及 dll) / `apk/`(ADBKeyBoard.apk, clipper.apk) / `ut/`(测试脚本)。
- 修复内容：主程序与 5 个 ut 测试脚本里所有 `scrcpy.exe`、`adb`、`.apk`、运行时文件(`admin_pwd.dat`/`zhibodou_config.json`/授权文件) 的路径，统一改为**基于 `__file__` 推导**（THIS_DIR=src, BASE_DIR=项目根），不再依赖 `os.getcwd()` 或 PATH。
- `adb` 调用全部改为完整路径 `scrcpy/adb.exe`；scrcpy 启动 `cwd` 设为 `scrcpy/` 目录以便加载其 dll。
- `ut/multiline_e2e_test.py` 与 `ut/final_production_test.py` 会 `exec` 主程序源码头部，已在 exec 命名空间注入 `__file__`，否则新增的 `__file__` 路径推导会 NameError。
- 新增 `requirements.txt`（pyautogui/pyttsx3/websocket-client/pyperclip/pygetwindow；tkinter 为 Python 标准库）。
- 验证：`py_compile` 全部通过；路径推导均指向真实存在的文件。依赖安装未能在沙箱内联网验证（PyPI 不可达），需用户本机 `pip install -r requirements.txt`。
- 坑：本机 Python 为 3.12（`C:\Users\Administrator\AppData\Local\Programs\Python\Python312`），原 `.bat` 写死的 Python311 不存在，已改为自动探测 PATH→Python312 回退。

## 单体重构为单职责多模块（src 拆分）
- 把 `src/zhibodou_full.py`(1100行) 按功能拆成 18 个模块，入口 `src/main.py`，旧单体归档为 `zhibodou_full.py.bak`。
- 模块划分：`paths`(路径) `state`(全局状态，`state.xxx` 替代 global) `doubao`(控件锚点常量) `config`(配置读写) `auth`(授权/密码) `adb_utils`(adb封装) `win_embed`(Windows嵌入API) `ui_locator`(uiautomator坐标定位) `input_text`(ADBKeyboard直输+发送入口) `scrcpy_embed`(投屏启停嵌入) `danmu`(弹幕解析+WS) `capture`(抓屏) `tts`(语音) `live`(直播控制) `power`(电源/自检) `dialogs`(弹窗) `ui`(界面构建,控件以`ui.xxx`暴露) `main`(入口接线)。
- 设计要点：依赖图为 DAG 无环；`ui` 不反向依赖业务逻辑，按钮回调在 `main.py` 统一接线；`adb devices` 必须用 `run_adb(['devices'])`，曾误用 `adb_shell('devices')`(变 `adb shell devices` 无效)导致自检误报无设备，已修。
- `ut` 两个 exec 型测试改为直接 `import` 新模块（桩掉 tkinter/pyautogui 等），不再依赖单体文件。
- `.bat` 改为启动 `python main.py`（仍 GBK+CRLF）。
- 验证：全部模块 `py_compile` 通过；用桩模块(虚拟 pyautogui/pyttsx3/websocket/pygetwindow)验证整条导入链无循环依赖、无符号缺失；`load_config`/`state`/`send_text_to_doubao("")` 等纯逻辑正确；真机已连(设备号 R58R76D5MC1，豆包在前台)，`adb_devices_online()` 修复后正确返回。
- 模块设计文档：`E:\zhibodou-ai\zhibodou\MODULES.md`。

## 多模块进一步归并为功能包（src 包化）
- 把扁平的 18 个模块按功能域归并到 7 个目录包：`core/`(paths,state,doubao) `settings/`(config,auth) `device/`(adb_utils,ui_locator,input_text) `screen/`(win_embed,scrcpy_embed,capture,danmu) `broadcast/`(live,power) `audio/`(tts) `gui/`(ui,dialogs)。每个包含 `__init__.py`；`main.py` 仍在 src 根。
- 所有跨模块 import 改为以 `src/` 为根的绝对导入（如 `from gui.ui import set_status`、`from settings.config import load_config`、`from device.adb_utils import adb_shell`）；`main.py` 开头 `sys.path.insert(0, 自身目录)` 保证双击 bat(cwd=src) 或从项目根 `python src/main.py` 都能解析包。
- 一次性脚本 `_pkg_refactor.py` 完成：建包目录+`__init__.py`、移动文件、批量改写 import、对源文件做快照 `_flat_snapshot`（验证后已删除）；`.stubs/` 桩模块仅用于导入链验证，验证后删除。
- 验证：全部模块 `py_compile` 通过；桩环境 `import main` 拉起所有包模块无循环依赖/无缺失符号；`paths.SCRCPY_EXE` 等纯逻辑正确；`ut` 两测试改为 `from device.input_text import ...` 等包导入并编译通过。
- `MODULES.md` 已更新目录树、包归属列、包化依赖图与导入约定。

## 新增豆包就绪检测 + 修复 paths 路径回归
- 新增 `device/doubao_check.py`：只读体检 `check_doubao_ready()`，覆盖用户要求的四项（其实五项）：①adb设备在线 ②豆包已运行且真在前台 ③处于对话界面(控件树有 `DOUBAO_INPUT_ID`) ④界面无遮挡(`mCurrentFocus`/`TYPE_SYSTEM_ALERT`/`TYPE_APPLICATION_OVERLAY` 不是豆包) ⑤可发送(输入框存在且 enabled)。全部只读，不点击/不输入，避免干扰真实对话。辅助函数 `doubao_running/in_conversation_screen/is_occluded/can_send`。
- 强化 `device/adb_utils.doubao_in_foreground()`：旧版只查 `dumpsys window` 含包名（后台也会命中误判前台）；改为优先查 `mResumedActivity`/`mTopResumedActivity`/`mFocusedApp`，兜底回退 window。
- `broadcast/power.power_on_self_check()` 接入 `check_doubao_ready()`；新增 `startup_readiness_check(log=True)`，在 `main.py` 建 UI 后由后台线程调用，结果写 `gui.ui.log_screen()` + 状态栏。为此给 `gui/ui.py` 加 `log_screen()` 线程安全追加日志函数。
- **顺带修了一个包化遗留的回归**：`core/paths.py` 被移到 `src/core/` 后，`THIS_DIR=src/core`、`BASE_DIR=src`，导致 `ADB_EXE/SCRCPY_EXE/APK/运行时文件` 全部算错一级（`src/scrcpy/adb.exe` 不存在 → FileNotFoundError）。改为「向上查找同时含 `scrcpy/` 与 `src/` 的目录」作为 `BASE_DIR`，`THIS_DIR=SRC_DIR`(src)。真机跑 `check_doubao_ready()` 验证：设备 R58R76D5MC1 在线、豆包前台=True，但当前通知栏(NotificationShade)下拉盖住界面 → 正确报「被遮挡」「不在对话界面」，证明检测可用。
- 全部 19 个 src 模块 + 2 个 ut 测试 `py_compile` 通过；`MODULES.md` 已补 `doubao_check.py` 与新流程说明。


## 灭屏/锁屏自动唤醒拉起豆包（用户需求：屏幕熄灭时能否自动打开豆包）
- 新增 `device/wake_unlock.py`：①`is_screen_off()`(`dumpsys power` 读 `mWakefulness=Asleep/Dozing`，兜底 `mDreamingLockscreen`) ②`is_keyguard_locked()`(`dumpsys window policy` 读 `STATE_UNLOCKED`/`isStatusBarKeyguard`/`mInputRestricted`) ③`wake_screen()`(`input keyevent 26`) ④`launch_doubao()`(`monkey -p com.larus.nova -c LAUNCHER 1`，回退 `am start`) ⑤`enter_first_conversation()`(已在对话页直接 True；否则轻量尝试点含"对话/历史"文案或 RecyclerView 首项进入对话) ⑥`ensure_doubao_awake_and_foreground()` 一键恢复，返回 `(ok,msg,need_manual_unlock)`。
- **能力边界（已与用户确认其机器为无锁屏/滑动锁屏）**：无锁屏/滑动锁屏 -> 全自动亮屏+拉起豆包到对话页；PIN/密码/图案锁屏 -> adb 无法自动解，只能亮屏停在解锁界面，返回 `need_manual_unlock=True` 让用户手动解锁后程序继续（Android 安全机制，绕不过）。
- 接入两处：① `check_doubao_ready()` 在线分支最前加灭屏优先检测——灭屏/锁屏先 `ensure_doubao_awake_and_foreground()`，成功记 i，锁屏则本次标未就绪并提示手动解锁；② `input_text.send_text_to_doubao()` 发送前先恢复灭屏（避免黑屏假阴性误报，锁屏时报错让调用方重试）。
- 验证：三文件 `py_compile` 通过；桩环境(虚拟 tkinter/pyautogui 等) 导入链全通、6 个唤醒函数齐全；`MODULES.md` 已补新模块、调用链与流程说明。真机未跑（沙箱无设备），需用户本机连三星 R58R76D5MC1 实测灭屏自动拉起效果。
